{
  "name": "foundation-ingest",
  "version": "1.0.0",
  "scripts": {
    "build": "docker build -t $npm_package_name . && docker tag $npm_package_name:latest lifeomic/$npm_package_name:$npm_package_version"
  },
  "devDependencies": {
    "@lifeomic/dev-tools": "^4.9.3"
  }
}
